struct PortId_t {
       }
        header H {
       }
        struct metadata_t {
       }
        control I(inout metadata_t meta)() {
         apply {
          if (((meta.foo) == (PSA_CPU_PORT))) {
     }
        }
       }
