    struct Headers {
       }
        control c(in Headers h)() {
         action a_params(bit<32> param) {
     }
         table t_exact_ternary {
          actions = {
         }
          largest_priority_wins = "a_params";
        }
         apply {
     }
       }
